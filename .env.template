# =================================================================
# ReFinED - Environment Configuration Template (Compose + API)
# Instructions: Copy this file to '.env' in the repository root.
# =================================================================

# --- SECURITY ---
# Your secret key for API access.
# One way to generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your-super-secret-key

# --- HARDWARE & PERFORMANCE ---
# Options: 'cuda' (gpu) or 'cpu'.
MODEL_DEVICE=cpu
# Set to 'cuda' when using docker-compose.gpu.yml on NVIDIA-enabled hosts.

# --- SERVER SETTINGS ---
# Host port exposed by docker-compose.
API_PORT=8002

# Internal API service port. Leave at 8002 for Docker unless you intentionally
# change the container command and port mapping.
FASTAPI_SERVER_PORT=8002

# Enable debug mode for verbose logging and interactive Swagger docs.
# Options: True / False
DEBUG=False

# --- DEVELOPMENT & TESTING ---
# If set to True, the API returns fake data and does NOT load the heavy model.
# Useful for frontend testing or UI development without a GPU.
MOCK_MODE=False
