# =================================================================
# ReFinED API - Environment Configuration Template
# Instructions: Copy this file to '.env' and fill in your values.
# =================================================================

# --- SECURITY ---
# Your secret key for API access.
# One way to generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=your-super-secret-key

# --- HARDWARE & PERFORMANCE ---
# Options: 'cuda' (gpu) or 'cpu'.
MODEL_DEVICE=cpu

# --- SERVER SETTINGS ---
# The port FastAPI will listen on.
FASTAPI_SERVER_PORT=8002

# Enable debug mode for verbose logging and interactive Swagger docs.
# Options: True / False
DEBUG=False

# --- DEVELOPMENT & TESTING ---
# If set to True, the API returns fake data and does NOT load the heavy model.
# Useful for frontend testing or UI development without a GPU.
MOCK_MODE=False